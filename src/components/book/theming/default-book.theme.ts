import styles from './default-book.module.css';

/**
 * Default theming for the book.
 * This uses the '> *' selector to target page elements.
 * ::before is used for border.
 */
const DefaultBookThemeClassName = styles.defaultBookTheme;

export default DefaultBookThemeClassName;

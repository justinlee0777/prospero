export default interface NewlineTransformerOptions {
  /** The number of newlines that start a section. */
  beginningSections?: number;
  /** The number of newlines between paragraphs. Default is 1. */
  betweenParagraphs?: number;
}
